{% extends 'layout.html.twig' %} 
{% block body %}
	<div class="container-fluid">
		<div class="row">
        	<div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
          		<h1 class="page-header">Wybierz auto</h1>
          		<div class="table-responsive">
            		<table class="table table-striped">
              			<thead>
                			<tr>                  				
                  				<th>Nazwa</th>
                  				<th>Segment</th>
                  				<th>Cena za dzień</th>
								<th></th>
                			</tr>
              			</thead>
              			<tbody>

						{% for car in cars %}
						<tr>
                  			<td>{{ car.name }}</td>
                  			<td>{{ car.segment }}</td>
                  			<td>{{ car.price }}</td>
							<td><a href="{{url('wypozycz_auto',{'id': car.id})}}">Rezerwuj</a></td>
						</tr>
 						{% endfor %}
              			</tbody>
            		</table>
          		</div>
    		</div>
    	</div>
    </div>
{% endblock %}

